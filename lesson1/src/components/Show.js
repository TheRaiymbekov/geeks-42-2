import React from 'react';

const Show = ({data}) => {
    return (
        <div style={{color: 'yellow' +
                ''}}>
            {
               data
            }
        </div>
    );
};

export default Show;