import React from 'react';

const Show = ({title}) => {
    return (
        <div style={{color: 'yellow' + ''}}>
            {
                title
            }
        </div>
    );
};

export default Show;